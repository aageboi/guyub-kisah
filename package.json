{
  "name": "amp-story",
  "version": "1.0.0",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next build && next export -o _static",
    "deploy": "next build && next export -o _static && scp -P22055 -r _static/* aageboi@jingga:/var/www/kisah.guyub.id/"
  },
  "dependencies": {
    "next": "latest",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "swr": "^0.5.5"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/react": "^17.0.3",
    "typescript": "^4.2.4"
  }
}
